RF database

1.base on sqlalchemy
2.migration control by http://code.google.com/p/sqlalchemy-migrate/
3.setup tools pbr from OpenStack




2013/7/17 develop and install env
=================================
svn reversion 1595
a) create babel.cfg, requirements.txt setup.cfg setup.py in the project directory
b) smoke_sh. : setup for development
        python setup.py egg_info; \
        raw_links=$(awk '/^.+/ {print "-f " $1}' *.egg-info/dependency_links.txt); \
        depend_links=$(echo $raw_links | xargs); \
        require_file=$([ ! -r *-info/requires.txt ] || echo "-r *-info/requires.txt"); \
        pip_install $require_file $depend_links;\
        sudo python setup.py develop



2013/07/16 Initial version
==========================
svn version: 1589

this is the first thing we had, nothing, just migrate configure by 
sqlalchemy-migrate

cd rf_db
a) migrate create regen/sqlalchemy/migrate_repo "RF DB project"
get regen/sqlalchemy/migrate_rep/versions
    regen/sqlalchemy/migrate_rep/manage.py
    regen/sqlalchemy/migrate_rep/migrate.cfg
    regen/sqlalchemy/migrate_rep/README

    

